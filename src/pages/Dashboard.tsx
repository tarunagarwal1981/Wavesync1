const Dashboard = () => {
  console.log('Dashboard rendering after icon fixes');
  
  return (
    <div style={{ padding: '20px', backgroundColor: 'lightgreen' }}>
      <h1>Dashboard Working After Icon Fixes</h1>
      <p>If you see this, Dashboard is rendering</p>
    </div>
  );
};

export default Dashboard;